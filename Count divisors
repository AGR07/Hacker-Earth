#include<iostream>
using namespace std;

int check(int x ,int y, int z)
{
	int count = 0;
	for (int i = x ; i <= y ; i++)
	{
		if(i % z == 0)
		{
			count++;
		}
	}
	return count;
}

int main()
{
	int l ,r, k;
	cin >> l >> r >> k;
	cout << check(l,r,k) <<endl;
}
