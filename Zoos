#include<iostream>
#include<string>
using namespace std;

int main()
{
	string s;
	cin >> s;
	int l = s.length();
	int x=0,y=0;
	for(int i = 0 ; i < l ; i++)
	{
		if(s[i] == 'z')
		{
			x++;
		}
		else{
			y++;
		}
	}
	if((2*x) == y)
	{
		cout << "Yes"<<endl;
	}
	else{
		cout << "No" << endl;
	}
}
